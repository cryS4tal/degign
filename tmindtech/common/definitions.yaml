error:
  description: 错误消息
  type: object
  properties:
    code:
      description: 错误码
      type: string
    message:
      description: 错误消息
      type: string
    debug_message:
      description: 调试信息
      type: string
id:
  type: integer
  format: int64
  description: 唯一标示
array_base:
  type: object
  properties:
    offset:
      type: integer
      description: 分页偏移
    count:
      type: integer
      description: 列表数量
    total_count:
      type: integer
      description: 总数
create_time:
  type: string
  description: 创建时间
  format: 'date-time'
modify_time:
  type: string
  description: 修改时间
  format: 'date-time'
json_str:
  type: string
  description: JSON数据
