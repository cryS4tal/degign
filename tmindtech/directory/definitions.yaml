node:
  type: object
  description: 节点
  properties:
    id:
      $ref: '#/definitions/id'
    type:
      type: integer
      description: 节点类型
    name:
      type: string
      description: 名称
    description:
      type: string
      description: 描述
    extras:
      $ref: '#/definitions/json_str'
    path:
      type: string
      description: 路径
dir_base:
  type: object
  description: 目录
  allOf:
    - $ref: '#/definitions/node'
    - type: object
      description: 节点列表
      properties:
        nodes:
          type: array
          items:
            $ref: '#/definitions/node'
simple_dir:
  type: object
  description: 目录
  properties:
    path:
      type: string
      description: 路径
    name:
      type: string
      description: 名称


