node:
  type: object
  description: 节点
  properties:
    id:
      $ref: '#/definitions/id'
    name:
      type: string
      description: 名称
    description:
      type: string
      description: 描述
    path:
      type: string
      description: 路径

nodes:
  description: 节点列表
  type: object
  allOf:
    - $ref: '#/definitions/array_base'
    - type: object
      properties:
        data_list:
          type: object
          items:
            $ref: '#/definitions/node'
dir_base:
  type: object
  description: 目录
  allOf:
    - $ref: '#/definitions/node'
    - type: object
      description: 节点列表
      properties:
        subfolders:
          $ref: '#/definitions/nodes'


