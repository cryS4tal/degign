node:
  type: object
  description: 节点
  properties:
    id:
      $ref: '#/definitions/id'
    type:
      type: integer
      description: 节点类型
    name:
      type: string
      description: 名称
    description:
      type: string
      description: 描述
    extras:
      $ref: '#/definitions/json_str'
    path:
      type: string
      description: 路径

nodes:
  description: 节点列表
  type: object
  allOf:
    - $ref: '#/definitions/array_base'
    - type: object
      properties:
        data_list:
          type: array
          items:
            $ref: '#/definitions/node'
dir_base:
  type: object
  description: 目录
  allOf:
    - $ref: '#/definitions/node'
    - type: object
      description: 节点列表
      properties:
        nodes:
          $ref: '#/definitions/nodes'


