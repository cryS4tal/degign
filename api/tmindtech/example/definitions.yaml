example:
  type: object
  properties:
    id:
      $ref: '#/definitions/id'
    name:
      type: string
      description: 名称
    description:
      type: string
      description: 通知描述
    extras:
      description: 额外信息
      allOf:
        - $ref: '#/definitions/json_str'
examples:
  type: object
  allOf:
    - $ref: '#/definitions/array_base'
    - type: object
      properties:
        data_list:
          type: array
          items:
            $ref: '#/definitions/example'
